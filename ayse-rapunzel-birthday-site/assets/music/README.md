# Müzik

Site varsayılan olarak tarayıcı içinde yumuşak bir melodi üretir.

Kendi dosyanı kullanmak için örnek:

1. `ayse.mp3` dosyasını bu klasöre koy.
2. `script.js` içinde `CUSTOM_MUSIC_FILE` satırını şöyle değiştir:

```js
const CUSTOM_MUSIC_FILE = "assets/music/ayse.mp3";
```
